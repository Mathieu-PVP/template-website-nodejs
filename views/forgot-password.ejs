<%- include('./layouts/header.ejs') %>

        <div class="container">

            <% if (messages.error) { %>
            <div class="alert alert-danger" role="alert">
                <p><%= messages.error %></p>
            </div>
            <% } %>

            <% if (messages.success) { %>
            <div class="alert alert-success" role="alert">
                <p><%= messages.success %></p>
            </div>
            <% } %>

            <h2 class="text-center">Réinitialiser mon mot de passe</h2>

            <form method="post" action="/auth/forgot-password" class="shadow-sm p-3 mb-5 bg-body-tertiary rounded">
                <div class="mb-3">
                    <label for="email" class="form-label">Adresse e-mail</label>
                    <input type="email" class="form-control" aria-describedby="emailHelp" name="email" value="<%= email %>">
                    <div class="form-text">Nous ne partagerons jamais vos informations personnelles.</div>
                </div>
                <button type="submit" class="btn btn-primary">Envoyer</button>
            </form>

        </div>

<%- include('./layouts/footer.ejs') %>